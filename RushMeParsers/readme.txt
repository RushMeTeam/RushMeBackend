resizing.py requires PIL to be installed to run, takes in an image
as a command line argument and resizes it and creates the two resized images
in 1080p and 480p in the same directory.

fratjson.py has 2 functions: parseFraternities and parseEvents
it is a parser that returns a json object with the info it parses